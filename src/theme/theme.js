import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      light: '#64cfbf',
      main: '#2a9d8f',
      dark: '#006e62',
      contrastText: '#fff',
    },
  },
});

export default theme;
